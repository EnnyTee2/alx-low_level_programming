#include<stdio.h>

/**
 * _isupper - function that tests input whether it is Upper case
 *
 * Return: 1 if uppercase, 0 if otherwise
 */

int _isupper(int c)
{
	int rev;

	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	{
	return (0);	
}
